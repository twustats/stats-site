export type Theme = {
  background: string;
  foreground: string;
  accent: string;
  font: string;
};

export const theme: Theme = {
  background: "#ffffff",
  foreground: "#0a0a0a",
  accent: "#a855f7",
  font: "system-ui, sans-serif",
};
