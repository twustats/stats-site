export type Theme = {
  background: string;
  foreground: string;
  accent: string;
  font: string;
};

export const theme: Theme = {
  background: "#ffffff",
  foreground: "#0a0a0a",
  accent: "#22c55e",
  font: "system-ui, sans-serif",
};
