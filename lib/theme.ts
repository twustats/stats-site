export type Theme = {
  background: string;
  foreground: string;
  accent: string;
  font: string;
};

export const theme: Theme = {
  background: "#808080",
  foreground: "#ff0000",
  accent: "#a855f7",
  font: "system-ui, sans-serif",
};
