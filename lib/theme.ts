export type Theme = {
  background: string;
  foreground: string;
  accent: string;
  font: string;
};

export const theme: Theme = {
  background: "#000000",
  foreground: "#ffffff",
  accent: "#a855f7",
  font: "system-ui, sans-serif",
};
